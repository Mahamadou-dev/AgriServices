# Server Configuration
PORT=3001

# MongoDB Configuration
MONGODB_URI=mongodb://mongodb:27017/farmers_db

# JWT Configuration (must match auth-service secret)
# SECURITY: Generate a secure secret with: openssl rand -base64 64
# This MUST be the same secret used in all services (auth-service, prediction-service, etc.)
JWT_SECRET=VOTRE_SECRET_JWT_ICI_MINIMUM_256_BITS

# Node Environment
NODE_ENV=production
